## Setup:

    npm install

## To run:

    cd client
    npm run dev

Then go to http://localhost:5001

## To build:
    
    cd client
    npm run build

Then host the buiild folder from a static web server.
