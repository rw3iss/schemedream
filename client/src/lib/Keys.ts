/*
 * Keys - A dictionary of app-wide LocalStorage keys.
 */
export default class Keys {

    static RECENT_PROJECT = 'recent-project';

    static PROJECTS = 'projects';

    static PROJECT_PREFIX = 'projects#';

}